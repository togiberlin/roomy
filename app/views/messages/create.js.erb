// Use private pub gem to allow real-time message updates
<% publish_to conversation_messages_path(@conversation) do %>
  // After sending message, append message without reloading page (AJAX)
  $('#chat').prepend('<%= j render @message %>');
<% end %>

// Reset the text area field
$('#new_message')[0].reset();
